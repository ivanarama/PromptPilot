name: Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build-exe:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pyinstaller

      - name: Build exe
        run: python -m PyInstaller pp.spec --clean

      - name: Create Windows archive
        run: |
          Copy-Item .env.example dist\.env
          Compress-Archive -Path dist\pp.exe, dist\.env, start.ps1, stop.ps1 `
            -DestinationPath "PromptPilot-${{ github.ref_name }}-windows.zip"

      - uses: actions/upload-artifact@v4
        with:
          name: windows-zip
          path: "PromptPilot-${{ github.ref_name }}-windows.zip"

  build-exe-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pyinstaller

      - name: Build binary
        run: python -m PyInstaller pp.spec --clean

      - name: Create Linux archive
        env:
          REF_NAME: ${{ github.ref_name }}
        run: |
          cp .env.example dist/.env
          tar -czf "PromptPilot-${REF_NAME}-linux.tar.gz" \
            -C dist pp .env

      - uses: actions/upload-artifact@v4
        with:
          name: linux-tar
          path: "PromptPilot-${{ github.ref_name }}-linux.tar.gz"

  build-wheel:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build wheel
        run: |
          pip install build
          python -m build --wheel --sdist

      - uses: actions/upload-artifact@v4
        with:
          name: python-dist
          path: dist/

  release:
    needs: [build-exe, build-exe-linux, build-wheel]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: windows-zip

      - uses: actions/download-artifact@v4
        with:
          name: linux-tar

      - uses: actions/download-artifact@v4
        with:
          name: python-dist
          path: python-dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "PromptPilot ${{ github.ref_name }}"
          body: |
            ## Установка

            ### Windows (exe)
            1. Скачай `PromptPilot-${{ github.ref_name }}-windows.zip`
            2. Распакуй в любую папку
            3. Заполни `.env` (шаблон уже внутри архива)
            4. Запусти `start.ps1` или `pp.exe server`

            ### Linux (standalone binary)
            ```bash
            tar -xzf PromptPilot-${{ github.ref_name }}-linux.tar.gz
            cp .env.example .env  # заполнить при необходимости
            ./pp server
            ```

            ### Docker
            ```bash
            docker compose up -d --build
            # Web UI: http://localhost:8420
            ```

            ### Linux / Mac / Python
            ```bash
            pip install promptpilot-*.whl
            pp server
            ```
            Или из исходников:
            ```bash
            pip install git+https://github.com/${{ github.repository }}.git@${{ github.ref_name }}
            ```
          files: |
            PromptPilot-${{ github.ref_name }}-windows.zip
            PromptPilot-${{ github.ref_name }}-linux.tar.gz
            python-dist/*.whl
            python-dist/*.tar.gz
          draft: false
          prerelease: false
